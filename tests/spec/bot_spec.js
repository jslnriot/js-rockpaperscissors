describe ("Bot", function(){
  it("Should return either rock paper or scissors from the results array", function(){
    //expect(Bot).toContain(['Rock', 'Paper', 'Scissors']);

    //var arr = Bot;
    expect(bot1()).toEqual("hello");
  });

});
